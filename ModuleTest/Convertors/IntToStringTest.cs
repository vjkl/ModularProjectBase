using Module.Convertors;
using System;
using System.Collections.Generic;
using Xunit;

namespace ModuleTest.Convertors
{
  public class IntToStringTest
  {
    //private IntToString intToString = new IntToString();

    //[Fact]
    //public void ConvertTest ()
    //{
    //  List<int> listInt = new List<int>();

    //  object expectedObj = intToString.Convert(string.Empty, null, null, null);
    //  Assert.Null(expectedObj);

    //  expectedObj = intToString.Convert(listInt, null, null, null);
    //  Assert.Null(expectedObj);

    //  listInt.Add(1);
    //  expectedObj = Convert.ToString(intToString.Convert(listInt, null, null, null));
    //  string expected = Convert.ToString(expectedObj);
    //  Assert.Equal("[1,1]", expected);
    //}
  }
}
